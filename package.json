{
  "name": "jaywardhan-website",
  "version": "1.0.0",
  "description": "Personal portfolio site (React) with an admin panel for editing content and pages",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "dev": "concurrently -n server,client -c blue,green \"npm run server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
